currently, we have a new FinRL specific python environment (running on python 3.10).
Using Python 3.10.18 environment at: .venv_finrl

IF there are bad imports, try these:
install in this order after activating the new environment:

uv pip install "urllib3==1.26.18"
uv pip install "requests==2.31.0" 
uv pip install "alpaca-trade-api==3.2.0"
uv pip install finrl
uv pip install pyfolio
uv pip install gymnasium
uv pip install stable_baselines3
uv pip install exchange-calendars
uv pip install stockstats==0.5.4
uv pip install wrds==3.4.0
uv pip install "yfinance==0.2.58"

then downgrade numpy: 
uv pip install "numpy<2.0"